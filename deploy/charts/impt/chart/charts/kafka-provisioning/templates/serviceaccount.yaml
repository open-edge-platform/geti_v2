---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "kafka-provisioning.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "kafka-provisioning.labels" . | nindent 4 }}
{{- if .Values.global.enable_local_image_pull_secret }}
imagePullSecrets:
  - name: regcred
{{- end }}
