task: instance_segmentation
capabilities:
  tiling: true

hyperparameters:
  evaluation:
    metric: f_measure
  training:
    max_epochs: 200
    early_stopping:
      enable: true
      patience: 10
